WINGS OVER L.A. — SMOOTHER CLOUD BUILD

Upload every file and folder in this package to the root of your GitHub Pages repository.
Keep the uploads folder and all filenames exactly as provided.

Changes in this build:
- Cloud footage converted to 59.94 FPS with interpolated frames.
- Only one cloud video is decoded instead of two copies playing simultaneously.
- Scroll/parallax work is synchronized to the browser's animation frames.
- Repeated autoplay polling was removed to reduce main-thread work.

After GitHub Pages deploys, refresh with Ctrl+F5 on Windows or Cmd+Shift+R on Mac.
